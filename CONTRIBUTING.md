# Contributing

Run `npm run build` and `npm run lint` before submitting changes. New illustrated components must expose a useful semantic control, include reduced-motion behavior, and keep artwork decomposed into named, animatable groups. Registry entries should declare only their direct dependencies.
